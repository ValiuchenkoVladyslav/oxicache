{
  "name": "oxicache",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "check": "bun run --cwd packages/client-ts check",
    "build": "bun run --cwd packages/client-ts build",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "husky": "^9.1.7"
  }
}
